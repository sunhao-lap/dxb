# 系统设计说明书

**项目名称**：基于智能优化算法的柔性作业车间调度系统

**项目代号**：SmartFJSP

**文档类型**：系统设计说明书

**版本**：V1.0

**日期**：2026 年 8 月

---

## 1. 系统概述

### 1.1 设计目标

本系统旨在为柔性作业车间调度问题（FJSP）提供一个集算例管理、算法求解、可视化对比、插单重调度于一体的 Web 应用。系统以智能优化算法（遗传算法、模拟退火、粒子群优化及混合算法）为核心求解引擎，以甘特图为主要可视化手段，帮助调度员快速生成高质量排程方案，帮助学习者对比不同算法的性能差异。

### 1.2 设计范围

系统覆盖从算例数据输入到排程方案输出的完整链路，包括：算例解析、参数配置、优化求解、方案解码、甘特图渲染、方案对比、插单重调度、结果导出。不包含与真实 MES/ERP 系统的对接，不包含多用户权限管理。

### 1.3 运行环境

- 操作系统：Windows 10/11、Ubuntu 20.04+、macOS 12+
- Python：3.10 及以上
- 内存：最低 4GB，推荐 8GB
- 浏览器：Chrome / Edge / Firefox 最新版
- 网络：首次安装需联网下载依赖，运行时可离线

---

## 2. 系统架构设计

### 2.1 分层架构

系统采用四层架构：

- **表现层**：Streamlit 多页面 Web 界面，负责用户交互与结果展示
- **服务层**：FastAPI RESTful API，负责请求路由、参数校验、任务调度
- **算法层**：智能优化算法引擎，负责问题求解与方案解码
- **数据层**：算例文件、配置文件、排程方案（内存 + CSV 导出 + 可选 SQLite）

### 2.2 模块划分

| 模块编号 | 模块名称 | 职责 |
|----------|----------|------|
| M1 | 算例管理模块 | 标准算例加载、自定义算例输入、数据校验与转换 |
| M2 | 参数配置模块 | 算法选择、超参数设置、优化目标选择 |
| M3 | 优化求解模块 | GA/SA/PSO/混合算法执行、收敛过程记录 |
| M4 | 方案解码模块 | 染色体到排程方案的转换、指标计算 |
| M5 | 可视化模块 | 甘特图渲染、收敛曲线、箱线图、指标对比表 |
| M6 | 方案对比模块 | 多方案加载、指标并排对比、甘特图对照 |
| M7 | 插单重调度模块 | 新工单插入、已加工工序冻结、联合重调度 |
| M8 | 结果导出模块 | CSV 明细表导出、甘特图 PNG 导出、性能报告生成 |
| M9 | API 服务模块 | FastAPI 路由、Pydantic 数据模型、异常处理 |

### 2.3 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 算法 | NumPy + 原生 Python | 向量化计算提升效率，无需额外依赖 |
| 后端 | FastAPI + Uvicorn | 异步支持、自动文档、Pydantic 校验 |
| 前端 | Streamlit | 纯 Python、多页面、内置组件丰富 |
| 图表 | Plotly | 交互式甘特图、缩放平移、悬停详情 |
| 数据 | Pandas + CSV + YAML | 轻量、无需数据库服务 |
| 测试 | Pytest | 生态成熟、断言简洁 |
| AI 编程 | Cursor + Aider | Cursor 负责编码，Aider 负责 Git 提交与重构 |

---

## 3. 功能模块详细设计

### 3.1 M1 算例管理模块

**功能描述**：提供标准算例的一键加载与自定义算例的输入功能。

**输入**：
- 标准算例：算例名称（FT06、FT10、LA01-LA40、MK01-MK10）
- 自定义算例：工件数、设备数、工艺路线矩阵、加工时间矩阵

**处理流程**：
1. 接收用户选择或输入
2. 标准算例从 data/instances/ 目录读取文本文件
3. 自定义算例校验数据完整性（每道工序至少有一台可选设备）
4. 转换为内部 FJSPInstance 数据结构
5. 返回算例基本信息供界面展示

**输出**：FJSPInstance 对象（含工件列表、设备列表、工序可选设备与加工时间）

**异常处理**：文件不存在时提示；自定义数据缺失时标注具体工件与工序。

### 3.2 M2 参数配置模块

**功能描述**：允许用户选择算法并配置超参数。

**输入**：算法类型、种群规模、迭代次数、交叉概率、变异概率、初始温度、冷却系数、随机种子、优化目标

**处理流程**：
1. 校验参数范围（种群规模 10-500，迭代次数 10-10000，概率 0-1）
2. 越界时给出警告并建议默认值
3. 组装为 Config 对象传递给求解模块

**输出**：Config 配置对象

**默认参数**：

| 参数 | GA 默认 | SA 默认 | PSO 默认 |
|------|---------|---------|----------|
| 种群/粒子数 | 100 | - | 50 |
| 迭代次数 | 200 | 1000 | 200 |
| 交叉概率 | 0.8 | - | - |
| 变异概率 | 0.1 | - | - |
| 初始温度 | - | 1000 | - |
| 冷却系数 | - | 0.95 | - |
| 惯性权重 | - | - | 0.7 |

### 3.3 M3 优化求解模块

**功能描述**：执行智能优化算法，返回最优排程方案与收敛过程。

**输入**：FJSPInstance、Config

**处理流程**：
1. 根据算法类型实例化对应求解器
2. 初始化种群/解
3. 迭代执行选择-交叉-变异（GA）/ 邻域搜索-接受（SA）/ 速度-位置更新（PSO）
4. 每代记录最优解与平均解
5. 达到终止条件后返回最优个体

**输出**：Schedule 方案对象 + 收敛曲线数据 + 求解耗时

**终止条件**：达到最大迭代次数，或连续 N 代无改进（SA）。

### 3.4 M4 方案解码模块

**功能描述**：将算法输出的染色体转换为可执行的排程方案，并计算评估指标。

**输入**：染色体（工序排列 + 设备分配）、FJSPInstance

**处理流程**：
1. 按工序排列顺序逐道工序调度
2. 对每道工序，在分配设备上寻找最早可插入的空闲时段
3. 满足前序工序完成时间约束
4. 记录每道工序的设备、开始时间、结束时间
5. 计算 Makespan、各设备利用率、最大负载、总拖期

**输出**：Schedule 对象（含工序调度明细列表 + 指标字典）

**指标定义**：
- Makespan = max(所有工序完成时间)
- 设备利用率 = 该设备加工总时间 / Makespan
- 最大负载 = max(各设备加工总时间)
- 总拖期 = sum(max(0, 工件完成时间 - 交期))

### 3.5 M5 可视化模块

**功能描述**：以甘特图和统计图表展示排程方案与算法性能。

**甘特图设计**：
- 横轴：时间
- 纵轴：设备编号
- 每个色块：一道工序，颜色按工件区分，标注工序号
- 悬停显示：工件号、工序号、设备、开始/结束时间、加工时长
- 支持缩放、平移、导出 PNG

**收敛曲线图**：横轴为迭代次数，纵轴为 Makespan，同时绘制最优解曲线与平均解曲线。

**箱线图**：多次独立运行的 Makespan 分布，对比算法稳定性。

### 3.6 M6 方案对比模块

**功能描述**：加载多个排程方案进行并排对比。

**输入**：两个及以上已保存的方案 ID

**处理流程**：
1. 从内存或文件加载方案
2. 计算各方案指标
3. 生成指标对比表
4. 渲染并排甘特图

**输出**：指标对比表 + 并排甘特图

### 3.7 M7 插单重调度模块

**功能描述**：在已有排程中插入紧急工单并重新调度。

**输入**：原方案、新工单数据、当前时间点

**处理流程**：
1. 标记当前时间点之前已开始或已完成的工序为"冻结"
2. 将未开始工序与新工单工序合并为待调度集合
3. 对冻结工序之后的时段执行重调度
4. 保留冻结工序不变
5. 生成新方案并标注与原方案的差异

**输出**：新排程方案 + 差异说明（新增工序、移动工序、受影响设备）

### 3.8 M8 结果导出模块

**功能描述**：将排程方案与分析结果导出为文件。

**导出格式**：
- CSV：工序明细表（工件号、工序号、设备、开始、结束、时长）
- PNG：甘特图图片
- HTML：性能分析报告（含指标表、收敛曲线、箱线图）

### 3.9 M9 API 服务模块

**接口清单**：

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /api/instances | 列出可用算例 |
| POST | /api/instance/custom | 提交自定义算例 |
| POST | /api/solve | 执行调度求解 |
| GET | /api/schedule/{id} | 获取排程方案明细 |
| POST | /api/compare | 多方案对比 |
| POST | /api/reschedule | 插单重调度 |
| GET | /api/export/{id}/{format} | 导出结果 |

---

## 4. 算法详细设计

### 4.1 FJSP 数学模型

**决策变量**：
- x_{ijk} ∈ {0,1}：工序 O_{ij} 是否在设备 k 上加工
- s_{ij} ≥ 0：工序 O_{ij} 开始时间
- c_{ij} ≥ 0：工序 O_{ij} 完成时间

**约束条件**：
- 工序顺序约束：c_{ij} ≤ s_{i,j+1}（同一工件工序不可并行）
- 设备互斥约束：同一设备同一时刻最多一道工序
- 设备分配约束：每道工序恰好选择一台可选设备
- 非中断约束：工序一旦开始不可暂停

**目标函数**：min Makespan = max(c_{ij})

### 4.2 遗传算法（GA）

**编码**：双层染色体。第一层为基于操作的排列（工件号重复出现，出现次数等于工序数），第二层为每道工序的设备选择编号。

**初始化**：60% 随机，40% 启发式（SPT 最短加工时间优先、EDD 最早交期优先）。

**选择**：锦标赛选择，锦标赛大小 3。

**交叉**：
- 工序层：POX（Precedence Operation Crossover），保证工序顺序合法性
- 设备层：均匀交叉

**变异**：
- 工序层：交换变异（随机交换两个位置）
- 设备层：在可选设备集合中随机重新选择

**适应度**：1 / Makespan。

**伪代码**：

```
输入：problem, pop_size, max_iter, pc, pm, seed
输出：best_schedule

population = initialize(problem, pop_size, seed)
evaluate(population)
best = get_best(population)
history = [best.makespan]

for iter = 1 to max_iter:
    offspring = []
    while len(offspring) < pop_size:
        p1, p2 = tournament_select(population, k=3)
        if random() < pc:
            c1, c2 = pox_crossover(p1, p2)
        else:
            c1, c2 = clone(p1), clone(p2)
        if random() < pm:
            swap_mutate(c1)
        if random() < pm:
            machine_mutate(c2, problem)
        offspring.extend([c1, c2])
    evaluate(offspring)
    population = elitist_replace(population, offspring, elite_rate=0.1)
    best = update_best(best, population)
    history.append(best.makespan)

return decode(best), history
```

**时间复杂度**：O(max_iter × pop_size × n × m)，其中 n 为工件数，m 为设备数。

### 4.3 模拟退火（SA）

**初始解**：随机生成一个合法排列与设备分配。

**邻域操作**（等概率随机选择一种）：
1. 交换变异：交换排列中两个位置
2. 插入变异：将一个位置的工序插入到另一位置
3. 设备重选：随机更换某道工序的分配设备

**接受准则**（Metropolis）：
- 新解更优：直接接受
- 新解更差：以概率 exp(-Δ / T) 接受，Δ = 新 Makespan - 旧 Makespan

**冷却策略**：T_{k+1} = α × T_k，α = 0.95。

**终止条件**：达到最大迭代次数，或连续 50 代无改进。

**时间复杂度**：O(max_iter × n × m)。

### 4.4 粒子群优化（PSO）

**离散化策略**：采用基于排序的 PSO。粒子位置为工序排列，速度表示为一组交换操作序列。位置更新时，按速度中的交换概率执行交换操作，同时受个体最优与全局最优引导。

**参数**：惯性权重 w=0.7，认知因子 c1=1.5，社会因子 c2=1.5。

**时间复杂度**：O(max_iter × pop_size × n × m)。

### 4.5 混合算法（GA+SA）

在 GA 每代变异完成后，对当代精英个体（前 10%）执行 SA 局部搜索（固定 20 步），利用 SA 的概率突跳能力帮助精英个体跳出局部最优。其余流程与标准 GA 一致。

**预期效果**：收敛速度略慢于纯 GA，但最终解质量更优。

### 4.6 主动调度解码算法

```
输入：operation_sequence, machine_assignment, problem
输出：schedule

machine_available_time = [0] * m  # 每台设备的可用时间
job_last_finish = [0] * n          # 每个工件上道工序完成时间
schedule = []

for each operation in operation_sequence:
    job_id, op_id = decode(operation)
    machine = machine_assignment[operation]
    duration = problem.processing_time[job_id][op_id][machine]
    earliest_start = max(machine_available_time[machine], job_last_finish[job_id])
    # 在设备上寻找最早可插入空闲时段（主动调度）
    start = find_earliest_slot(machine, earliest_start, duration)
    end = start + duration
    schedule.append({job_id, op_id, machine, start, end})
    machine_available_time[machine] = end
    job_last_finish[job_id] = end

return schedule
```

---

## 5. 数据结构设计

### 5.1 核心数据类

```python
@dataclass
class Operation:
    job_id: int
    op_id: int
    eligible_machines: list[int]      # 可选设备编号列表
    processing_times: list[float]     # 对应各设备的加工时间

@dataclass
class Job:
    job_id: int
    operations: list[Operation]
    due_date: float = float('inf')    # 交期（可选）

@dataclass
class FJSPInstance:
    name: str
    num_jobs: int
    num_machines: int
    jobs: list[Job]

@dataclass
class ScheduleItem:
    job_id: int
    op_id: int
    machine_id: int
    start_time: float
    end_time: float

@dataclass
class Schedule:
    items: list[ScheduleItem]
    makespan: float
    machine_utilization: list[float]
    max_load: float
    total_tardiness: float
```

### 5.2 算例文件格式

标准 FJSP 文本格式：

```
第1行：工件数 设备数
第2行起：工件号 工序数 [可选设备数 设备号 加工时间]...
```

示例：
```
6 6
1  3  2 1 3 2 5  1 3 4  1 2 6
2  2  1 1 4  1 4 2
3  4  2 1 2 3 1  1 2 3  1 4 5  1 5 2
...
```

### 5.3 配置文件格式（YAML）

```yaml
algorithm: ga
population_size: 100
max_iterations: 200
crossover_rate: 0.8
mutation_rate: 0.1
sa_initial_temp: 1000
sa_cooling_rate: 0.95
pso_inertia: 0.7
pso_cognitive: 1.5
pso_social: 1.5
random_seed: 42
objective: makespan
```

### 5.4 导出 CSV 字段

| 字段 | 类型 | 说明 |
|------|------|------|
| job_id | int | 工件编号 |
| operation_id | int | 工序序号 |
| machine_id | int | 分配设备 |
| start_time | float | 开始时间 |
| end_time | float | 结束时间 |
| duration | float | 加工时长 |

---

## 6. 用户界面设计

### 6.1 页面清单

| 页面 | 路径 | 核心功能 |
|------|------|----------|
| 调度工作台 | / | 算例选择、参数配置、运行求解、甘特图展示 |
| 算法对比 | /compare | 多算法多参数批量运行、指标对比、收敛曲线 |
| 插单重调度 | /reschedule | 原方案加载、新工单输入、重调度、差异展示 |
| 算例管理 | /instances | 算例列表查看、自定义算例输入 |
| 结果导出 | /export | 方案选择、格式选择、下载 |

### 6.2 调度工作台布局

```
┌─────────────────────────────────────────────────────┐
│  柔性车间调度工作台                                    │
├───────────────┬──────────────────────────────────────┤
│  左侧控制面板   │  右侧结果展示区                       │
│               │                                      │
│  算例选择 ▼    │  ┌────────────────────────────────┐  │
│  算法选择 ▼    │  │  甘特图（Plotly 交互）          │  │
│  参数表单      │  │                                │  │
│  [运行求解]    │  └────────────────────────────────┘  │
│               │                                      │
│  求解状态      │  指标卡：Makespan / 利用率 / 耗时    │
│  收敛曲线      │                                      │
└───────────────┴──────────────────────────────────────┘
```

### 6.3 交互流程

1. 用户进入工作台，选择算例（或输入自定义算例）
2. 选择算法，调整参数（可使用默认值）
3. 点击"运行求解"，显示进度条
4. 求解完成后自动渲染甘特图与指标卡
5. 用户可切换查看收敛曲线
6. 可保存当前方案用于对比或导出

---

## 7. 关键技术难点与解决方案

### 7.1 难点一：FJSP 编码的合法性维护

**问题**：工序排列染色体在交叉变异后可能违反工艺路线约束。

**解决方案**：采用基于操作的编码（工件号重复出现），配合 POX 交叉算子，从编码层面保证工艺顺序合法性。解码时按排列顺序依次调度，自动满足约束。

### 7.2 难点二：甘特图高效渲染

**问题**：大规模问题（如 30 工件 × 10 设备 = 数百道工序）的甘特图渲染可能卡顿。

**解决方案**：使用 Plotly 的 FigureWidget 增量更新；按设备分组绘制，避免重复计算；提供"简化模式"（仅显示色块不标注文字）提升渲染速度。

### 7.3 难点三：算法参数调优

**问题**：不同算例适合的参数差异大，默认参数可能效果不佳。

**解决方案**：提供参数建议（根据算例规模自动推荐种群规模与迭代次数）；在算法对比页面支持参数扫描，帮助用户找到合适配置。

### 7.4 难点四：插单重调度的稳定性

**问题**：重调度可能导致大量已排工序移动，影响生产稳定性。

**解决方案**：冻结已加工工序，仅重排未开始工序；提供"右移重调度"（仅推迟受影响工序，不改变未受影响工序）与"完全重调度"两种模式供选择。

### 7.5 难点五：AI 生成算法代码的正确性

**问题**：Vibe Coding 生成的算法代码可能存在逻辑错误（如交叉算子破坏约束、解码遗漏约束）。

**解决方案**：每个算法模块配套单元测试，用小规模算例（FT06，已知最优解 55）验证；AI 生成后立即运行测试，失败则用错误信息驱动迭代修正。

---

## 8. 测试方案

### 8.1 测试策略

采用单元测试 + 集成测试 + 验收测试三级策略。单元测试覆盖算法核心函数与数据结构；集成测试覆盖 API 接口与前后端联调；验收测试对照需求规格逐项验证。

### 8.2 单元测试用例

| 测试编号 | 测试对象 | 测试内容 | 预期结果 |
|----------|----------|----------|----------|
| UT-01 | 算例解析器 | 加载 FT06 算例 | 工件数=6，设备数=6，工序总数=26 |
| UT-02 | 算例解析器 | 格式错误文件 | 抛出异常并提示错误行号 |
| UT-03 | GA 初始化 | 种群生成 | 种群大小正确，每个个体合法 |
| UT-04 | POX 交叉 | 交叉后染色体 | 工序顺序合法，设备分配在可选范围内 |
| UT-05 | 解码算法 | 已知简单案例 | Makespan 计算正确，无工序重叠 |
| UT-06 | GA 求解 | FT06 固定种子 | Makespan ≤ 60，结果可复现 |
| UT-07 | SA 求解 | FT06 固定种子 | 收敛曲线单调不增（最优值） |
| UT-08 | 指标计算 | 已知方案 | 利用率、负载、拖期计算正确 |
| UT-09 | 插单模块 | 冻结工序验证 | 已完成工序时间不变 |
| UT-10 | CSV 导出 | 导出后重新读取 | 数据一致，无字段丢失 |

### 8.3 集成测试

- API 接口测试：使用 FastAPI TestClient 验证全部接口的正常与异常路径
- 前后端联调：通过 Streamlit 触发求解，验证数据流转完整
- 性能测试：FT10 求解时间 ≤ 30 秒，甘特图渲染 ≤ 2 秒

### 8.4 验收标准

| 编号 | 验收项 | 通过标准 |
|------|--------|----------|
| AT-01 | 算例加载 | FT06、FT10、LA01 可正确加载 |
| AT-02 | 优化求解 | GA 在 FT06 上 Makespan ≤ 60 |
| AT-03 | 甘特图 | 正确展示，交互正常 |
| AT-04 | 方案对比 | 两方案可并排对比 |
| AT-05 | 插单重调度 | 新工单插入后生成合法方案 |
| AT-06 | 结果导出 | CSV 与 PNG 可正常下载 |
| AT-07 | 可复现性 | 同种子同参数结果一致 |
| AT-08 | 单元测试 | 全部通过，覆盖率 ≥ 60% |

---

## 9. 开发计划

### 9.1 阶段划分

| 阶段 | 周次 | 工作内容 | 交付物 |
|------|------|----------|--------|
| 需求与设计 | 第1-2周 | 需求分析、算例调研、算法选型、系统设计 | 开题报告、设计说明书 |
| 算法开发 | 第3-5周 | 算例解析器、GA/SA/PSO、解码评估、单元测试 | 算法库 v1.0 |
| 后端开发 | 第6周 | FastAPI 接口、数据模型、异常处理 | API 服务 |
| 前端开发 | 第7-8周 | Streamlit 5 页面、甘特图、交互优化 | Web 应用 |
| 集成测试 | 第9周 | 联调、性能测试、Bug 修复、测试补充 | 稳定版本 |
| 实验与文档 | 第10周 | 批量实验、性能报告、README、使用手册 | 实验报告 + 文档 |
| 答辩准备 | 第11周 | 演示视频、答辩 PPT、模拟答辩 | 全部交付物 |

### 9.2 Vibe Coding 应用计划

| 环节 | 工具 | 应用方式 |
|------|------|----------|
| 算例解析 | Cursor | 描述文件格式，AI 生成解析与校验代码 |
| 算法实现 | Cursor Composer | 同时生成编码/交叉/变异/解码模块 |
| API 开发 | Cursor | 生成路由、Pydantic 模型、异常处理 |
| 甘特图 | Cursor Chat | 生成 Plotly 甘特图渲染与交互代码 |
| 单元测试 | Aider + DeepSeek | 批量生成测试用例 |
| 代码重构 | Aider | 描述重构意图，自动修改并提交 |
| 文档撰写 | ChatGPT | 生成 README 与 API 文档初稿 |

### 9.3 风险管理

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 算法收敛效果差 | 中 | 高 | 调整编码与算子，增加启发式初始化，延长迭代 |
| 甘特图渲染复杂 | 低 | 中 | 使用 Plotly 模板，提供简化模式 |
| 插单逻辑复杂 | 中 | 中 | 先实现冻结重排，差异分析降级为文字说明 |
| AI 代码有 Bug | 高 | 中 | 每模块配单元测试，小步迭代即时验证 |
| 时间不足 | 中 | 高 | 优先核心功能，对比与插单可降级 |

---

## 10. 参考文献

[1] Garey M R, Johnson D S, Sethi R. The complexity of flowshop and jobshop scheduling[J]. Mathematics of Operations Research, 1976, 1(2): 117-129.

[2] Brandimarte P. Routing and scheduling in a flexible job shop by tabu search[J]. Annals of Operations Research, 1993, 41(3): 157-183.

[3] Pezzella F, Morganti G, Ciaschetti G. A genetic algorithm for the flexible job-shop scheduling problem[J]. Computers & Operations Research, 2008, 35(10): 3202-3212.

[4] 张超勇, 饶运清, 李培根, 等. 柔性作业车间调度问题的改进遗传算法[J]. 计算机集成制造系统, 2008, 14(9): 1776-1782.

[5] Holland J H. Adaptation in natural and artificial systems[M]. University of Michigan Press, 1975.

[6] Kirkpatrick S, Gelatt C D, Vecchi M P. Optimization by simulated annealing[J]. Science, 1983, 220(4598): 671-680.

[7] Kennedy J, Eberhart R. Particle swarm optimization[C]//ICNN, 1995: 1942-1948.

[8] Fisher H, Thompson G L. Probabilistic learning combinations of local job-shop scheduling rules[M]//Industrial Scheduling. Prentice-Hall, 1963.

[9] Lawrence S. Resource constrained project scheduling: An experimental investigation of heuristic scheduling techniques[R]. Carnegie Mellon University, 1984.

[10] FastAPI Documentation[EB/OL]. https://fastapi.tiangolo.com, 2024.

[11] Streamlit Documentation[EB/OL]. https://docs.streamlit.io, 2024.

[12] Plotly Documentation[EB/OL]. https://plotly.com/python, 2024.

[13] Aider: AI pair programming in your terminal[EB/OL]. https://aider.chat, 2024.

[14] Vaithilingam P, Zhang T, Glassman E L. Grounded copilot: How programmers interact with code-generating models[C]//CHI, 2023.
